---
description: Job evaluation mode (read canonical template + references; write markdown report under job-evaluation-reports/)
globs:
  - "job descriptions/**"
  - "job descriptions-processed/**"
  - "job-evaluation-reports/**"
  - "JOB_EVALUATION_TEMPLATE.md"
  - "JOB_EVALUATION_REFERENCES.md"
  - "JOB_EVAL_CHAT_COMMAND.md"
alwaysApply: false
---

# Job evaluation (repo workflow)

If the user is doing a **job fit evaluation** (explicitly or implicitly) against a job description, you must:

1. Read and follow:
   - `JOB_EVALUATION_TEMPLATE.md`
   - `JOB_EVALUATION_REFERENCES.md`
2. Treat the job description as coming from:
   - an attached/`@`-referenced JD file under `job descriptions/**` or `job descriptions-processed/**`, OR
   - pasted JD text in chat (only if no JD file is referenced)
3. Produce the evaluation using the **exact PART 1–6 headings** defined in `JOB_EVALUATION_TEMPLATE.md` (v1.9.0+: PART 2 picks `primary_resume_for_verdict`; PART 5 has **five** headline verdict lines from **primary resume + portfolio**, including **Signal strength: Recruiter** / **Signal strength: HM** (fit-signal diagnostics, not predictions); **balanced risk tone**—acknowledge screening risks without overweighting negative prose; PART 3 journals for tuning, not inflating headline scores).
4. **Write the full report to disk** as a new markdown file under `job-evaluation-reports/` using the naming + collision rules in `JOB_EVALUATION_TEMPLATE.md` (do not only print the report in chat). Include **`model_used`** in **Header metadata** (v1.12.0+).

Chat output policy:

- Prefer a short confirmation in chat with the **repo-relative output path**, unless the user explicitly asks for the full report in chat.
