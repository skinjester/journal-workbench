Run a job evaluation using `JOB_EVALUATION_TEMPLATE.md` and `JOB_EVALUATION_REFERENCES.md`.

Requirements:

- Write the full report to `job-evaluation-reports/` using the naming/collision rules in `JOB_EVALUATION_TEMPLATE.md`.
- Use the exact `PART 1` through `PART 6` headings from the template.
- Keep the report blunt and realistic, but make it easy to scan:
  - short paragraphs
  - bullets where natural
  - clear `####` subheads inside dense sections
  - bold lead labels in resume-comparison sections
- Use both resume variants listed in `JOB_EVALUATION_REFERENCES.md`.
- Review all required portfolio URLs from `JOB_EVALUATION_REFERENCES.md` and account for them in header metadata.
- Include at least 2 short quotes total from summaries or dumps in PART 3, with file paths.

Job description input:
